import ScriptDB from '../index.js';

export default ScriptDB;
export const {
    Base,
    Database,
    MongoError,
    Schema,
    Util,
    set,
    add,
    all,
    subtract,
    fetch,
    type,
    push,
    has
} = ScriptDB;